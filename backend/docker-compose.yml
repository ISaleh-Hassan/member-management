version: '3.9'

services:
  postgres:
    container_name: "postgres"
    image: postgres
    ports:
      - "5432:5432"
    restart: always
    environment:
      - POSTGRES_DB=vgt
      - POSTGRES_PASSWORD=password